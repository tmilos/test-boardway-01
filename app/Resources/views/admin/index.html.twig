{% extends 'base.html.twig' %}

{% block content %}
    <h1>{{ company.name }}</h1>

    <h2>Employees</h2>
    <ul>
        {% for employee in employees %}
        <li>{{ employee.accountId }} : {{ employee.companyRoles|join(', ') }}</li>
        {% endfor %}
    </ul>
{% endblock %}
